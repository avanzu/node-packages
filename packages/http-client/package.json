{
    "name": "@avanzu/http-client",
    "version": "1.0.0",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "test": "jest",
        "build": "tsc -p tsconfig.build.json",
        "postbuild": "tsc-alias -p tsconfig.build.json",
        "prebuild": "rimraf ./dist",
        "prepublish": "npm run build"
    },
    "engines": {
        "node": ">=18"
    },
    "devDependencies": {
        "typescript": "^5.8.3",
        "jest": "^29.7.0"
    }
}
